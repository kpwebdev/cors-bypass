# This Project is created to check if we can bypass the "Same Origin Policy" from the front-end

```bash
# npm script to start the react
npm run start-react

# npm script to start the server
npm run start-server
```
